language: java
jdk: oraclejdk8
sudo: true
git:
  depth: 1
branches:
  only:
  - master

matrix:
  include:
    - env: OSX=10.11
      os: osx
      osx_image: osx10.11
      rvm: system

before_install:
  - brew install ant

install:
  - true

script:
  - ant -f build-mac-ios.xml -v
