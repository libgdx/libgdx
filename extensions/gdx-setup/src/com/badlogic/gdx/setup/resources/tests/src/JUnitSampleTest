package %PACKAGE%.core.sampletest;

import org.junit.Assert;
import org.junit.Test;

public class JUnitSampleTest {

	@Test
	public void testTheUnitTest() {
		Assert.assertTrue("This test cannot fail.", true);
	}
}