package com.badlogic.gdx.backends.gwt.preloader;

/**
 * Stub interface, just there to trigger the PreloaderGenerator so
 * we can automatically copy over assets from the Android project
 * and generate the assets.txt file.
 * @author mzechner
 *
 */
public interface PreloaderBundle {
}
